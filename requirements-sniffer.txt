# Optional dependency for the packet sniffer (uma_api/packet_sniffer.py).
# Only needed if you want sweepy to capture the real game client's traffic
# (e.g. to recover the Fuji/Showtime selected_difficulty_info encoding).
# Normal sweepy operation does NOT require this.
#
#   pip install -r requirements-sniffer.txt
#
# Pinned to match the DumpMaster(loop=...) API the addon uses. msgpack and
# pycryptodome (the actual decrypt) are already in requirements.txt.
mitmproxy==11.0.2
