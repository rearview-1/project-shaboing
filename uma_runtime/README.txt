This directory holds runtime data for the bot — career logs, captured sessions,
empirical calibrations, learned presets, etc. It is intentionally empty in a fresh
install.

The bot auto-creates everything it needs on first run, including the per-account
instance directory (named after the account's viewer_id once the bot authenticates).

You do not need to put anything here manually.
